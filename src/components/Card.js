import styled from 'styled-components';

const Card = styled.div`
  background: #FFFFFF;
  box-shadow: 0 2px 48px 0 rgba(176,192,237,0.50);
  border-radius: 12px;
  padding: 2rem;
  margin: 1rem 0;
`;

export default Card;
